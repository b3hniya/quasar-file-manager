export function messageMutation(state, message) {
  state.message = message;
}

export function typeMutation(state, type) {
  state.type = type;
}

export function componentDisplayMutation(state, display) {
  state.displayBar = display;
}
